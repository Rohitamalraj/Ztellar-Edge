[package]
name = "ztellar-synth-sip"
version = "0.1.0"
edition = "2021"
rust-version = "1.89.0"
publish = false

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
